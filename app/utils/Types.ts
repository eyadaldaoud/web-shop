export interface ThemeType  {
    light: string;
    dark: string;
    system: string;
}

export interface NavItem {
    title: string;
    link: string;
}

export interface Category {
    name: string
    _id: string,
}